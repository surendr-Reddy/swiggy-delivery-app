import { useParams } from "react-router-dom";
// Implement the rest menu here
const RestaurantMenu = () => {
  let resid = useParams();
  console.log(resid);
  return (
    <div>
      <h1>
        hi</h1>
    </div>
  );
};
export default RestaurantMenu;
